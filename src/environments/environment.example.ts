// In order to use correct API key for open weather, create environment.ts file in this directory with the exported environment variable like below
// In the environment variable, add your open weather api key in order for API calls to go through

export const environment = {
  production: false,
  apiKey: 'your-api-key',
};
